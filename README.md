# Nukefy VPN

Клиент sing-box для Android и Windows. Поддерживаются VLESS Reality, VMess, Trojan, Shadowsocks, Hysteria2, TUIC, WireGuard и AmneziaWG.

Автор: **@kayorisan**

## Скачать

Готовые файлы версии **1.0.1** лежат в [релизах](https://github.com/kayorissss/Nukefy-VPN/releases/latest):

- **NukefyVPN-android.apk** — поставить на телефон. Если Android ругается, разрешите установку из этого источника.
- **NukefyVPN-windows-x64.zip** — распаковать и запустить `nukefy_vpn.exe`.

После установки откройте Настройки — кнопка «Скачать ядро» теперь самая верхняя, иначе VPN не запустится. Скачивается официальный CLI-архив sing-box (`sing-box-*-android-arm64.tar.gz`, `sing-box-*-android-arm.tar.gz` для 32-битных, `sing-box-*-windows-amd64.zip`); APK приложения SFA, `SFW-*.exe`, `.deb` и `.rpm` из того же релиза не берутся. На Android системный VPN дополнительно требует файл `libbox.aar` при сборке; в этом релизе его нет, поэтому на телефоне сначала работает локальный прокси, если устройство его разрешает.

Первый запуск пустой: нет предустановленных подписок. Ссылку можно вставить, набрать или отсканировать QR. Данные остаются на устройстве — зашифрованный Hive, без аналитики, Firebase и внешних логов. Подписки качаются только с URL, который указал пользователь.

## Возможности

- Главный экран с кнопкой питания, статусом, таймером и живым трафиком
- Серверы: две большие кнопки «Вставить из буфера» и «QR-код» сверху; «Ввести вручную», «Импорт из файла» и «Проверить пинг» — в меню из трёх точек
- Строка подписки без ссылки: номер, название, «Обновить», иконка пинга; долгое нажатие открывает настройки, «Выше» и «Ниже» меняют порядок
- Пинг, сортировка, фильтр по стране, протоколу и доступности
- Антиглушилка: мосты, фрагментация TLS, MUX, приоритет мостов; технические ползунки MUX, фрагментации и MTU — в «Ещё»
- Глушилки: «Проверить сеть» пингует порт 443 у российских (`yandex.ru`, `vk.com`, `www.gosuslugi.ru`, `mail.ru`) и зарубежных хостов (`google.com`, `www.gstatic.com`, `update.miui.com`, `cloudflare.com`, `github.com`) и говорит, белые списки это или глушилок не видно
- Крупная статистика: скорость сейчас, большой график, время сессии, трафик за сессию и за всё время
- Маршрутизация: весь трафик, только заблокированные, всё кроме РФ, свои правила
- DNS DoH/DoT, локальный SOCKS `10808` и HTTP `10809`, TUN `172.19.0.1/30`
- На Android: VpnService, уведомление с отключением, автозапуск, плитка быстрых настроек, раздельное туннелирование, установка обновления
- На Windows: трей, сворачивание в трей, автозапуск через реестр
- Проверка обновлений по GitHub Releases, с прогрессом, размером и оставшимся временем
- Кнопка Telegram Proxy в настройках

## Сборка

Нужен Flutter **3.47** (Dart `>=3.13`) и JDK 17. Самим собирать не обязательно: файлы появляются в релизе сами.

```bash
flutter pub get
flutter test
flutter build apk --release
flutter build windows --release
```

Android: `minSdk 21`, `targetSdk 34`, `applicationId` `com.nukefy.vpn`. Если движок Flutter или плагин откажется от API 21, в `android/app/build.gradle.kts` замените `minSdk = 21` на `minSdk = maxOf(21, flutter.minSdkVersion)`.

Релизный APK сейчас подписывается debug-ключом, чтобы `flutter build apk` проходил без keystore. Для публикации замените `signingConfig` в `android/app/build.gradle.kts`.

## Ядро

Конфиг — sing-box **1.12**. На Windows ядро и `wintun.dll` скачиваются из официального релиза SagerNet/sing-box кнопкой в настройках и запускаются как `sing-box.exe run -c`. TUN может потребовать запуск от администратора.

Выбор архива строгий: имя должно начинаться с `sing-box-` и заканчиваться на `.tar.gz` (Android, Linux) или `.zip` (Windows), причём с точным платформенным суффиксом — `android-arm64`, `android-arm`, `android-386`, `android-amd64`, `windows-amd64`. Поэтому `SFA-*.apk`, `SFW-*.exe`, `.deb`, `.rpm`, а также `*-legacy-windows-7.zip`, `*-glibc.tar.gz` и `*-musl.tar.gz` в кандидаты не попадают. После успешной установки приложение пишет «Ядро установлено», а не путь к файлу. Закрытие окна сворачивает приложение в трей, если это не выключено в настройках.

На Android системный VPN работает только через `libbox.aar` в процессе приложения. Положите архив в `android/app/libs/libbox.aar` и пересоберите. Как собрать AAR: `tool/build_libbox.sh`. Без AAR приложение собирается, но честно сообщает, что TUN недоступен. Запуск скачанного бинарника — запасной путь локального прокси; если система его запрещает, ошибка показывается, а не маскируется под подключение.

## Обновления

Кнопка «Проверить обновления» смотрит `https://api.github.com/repos/kayorissss/Nukefy-VPN/releases/latest`. Если релиза нет (404), это считается «вы уже на последней версии». Если версия новее и в релизе есть `.apk` или Windows-файл, показывается окно с процентами, скоростью и оставшимся временем, затем файл скачивается.
