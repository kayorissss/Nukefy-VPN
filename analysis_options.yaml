include: package:flutter_lints/flutter.yaml

analyzer:
  exclude:
    - build/**
    - android/**
    - windows/**
    - preview/**

linter:
  rules:
    avoid_print: false
    use_build_context_synchronously: false
